[project]
name = "openenv-email-triage"
version = "0.1.0"
description = "OpenEnv Email Triage Agent"
dependencies = [
    "httpx",
    "openai"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
